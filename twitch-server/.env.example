# Server Configuration
PORT=3000
NODE_ENV=production

# Database Configuration (PostgreSQL)
# Uncomment and configure these to use PostgreSQL instead of in-memory storage
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=twitch_minecraft
# DB_USER=postgres
# DB_PASSWORD=your_password

# To switch to PostgreSQL:
# 1. Install PostgreSQL
# 2. Create database: CREATE DATABASE twitch_minecraft;
# 3. Uncomment DB_* variables above
# 4. In src/index.js, change:
#    const database = require('./database');
#    to:
#    const database = require('./database.postgres');
# 5. Call await database.initialize() before starting server
